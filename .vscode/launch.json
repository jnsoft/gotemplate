{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Main",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/src/main.go",
            "cwd": "${workspaceFolder}",
             // "env": {},
            "envFile": "${workspaceFolder}/.env",
            "args": [
              // "-v"
            ]
        }
    ]
}
